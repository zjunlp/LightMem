## 文档范围

* 更新 `README.md`，新增与修改与“全量测试”相关的使用说明。

* 优化现有文档结构不变，并补充“统一入口脚本”和“汇总结果说明”。

## README 更新项

* 在“⚡ Quick Start”与“✅ End-to-End Run”中新增统一入口：`python scripts/run_full_tests.py`。

* 新增“🧪 Full Test Runner（统一入口）”小节：

  * 说明交互式选择与命令行参数：`--target qwen|gpt|both|summarize_only`。

  * 提供 Windows/Powershell 与 Bash 示例代码块，标注语言避免 MD040。

  * 说明脚本会在结束时自动生成并打印汇总（`reports/summary.json`）。

* 在“📊 Results & Evaluation”中补充“汇总结果”说明，列出 `summary.json` 的关键字段与含义。

* 在“⚠️ Troubleshooting”中加入以下提示：

  * 当 `results` 目录为空时，汇总输出为 0（如何触发跑两种测设）。

  * 未安装 Qdrant 或未启用向量检索时，`vector_counts` 为 0 的原因。

  * Ark API/模型配置缺失导致脚本报错的定位方式（路径与行号）。

## 结果汇总说明（新增小节内容）

* 位置：README 的“📊 Results & Evaluation”末尾。

* 描述 `reports/summary.json` 字段：

  * `total_samples`、`correct_count`、`accuracy`、`avg_construction_time`、`total_vectors`、`avg_vectors_per_collection`。

  * `vector_counts` 为每个 `question_id` 的向量数量；`top_collections_by_vectors` 为 Top10。

  * `source` 字段包含输入与输出路径。

* 给出简短 JSON 示例（带 `json` 语言标记）。

## 使用示例（补充到 README）

* 交互式选择：

```powershell
python scripts/run_full_tests.py
```

* 指定测设：

```powershell
python scripts/run_full_tests.py --target qwen
python scripts/run_full_tests.py --target gpt
python scripts/run_full_tests.py --target both
python scripts/run_full_tests.py --target summarize_only
```

* 常见输出示例（摘录）并说明生成位置：`E:/.../LightMem/reports/summary.json`。

## 兼容性与注意事项

* 保持现有的单脚本运行路径与行为不变（`experiments/run_lightmem_qwen.py` 与 `experiments/run_lightmem_gpt.py` 仍可直接执行）。

* 统一结果目录为 `../results`，确保两脚本输出一致，便于汇总。

* 文档示例使用 Powershell/Bash 双语代码块，保证不触发 MD040；列表与段落之间添加空行以避免 MD032。

## 执行方式

* 我将在 `README.md` 中按上述位置与内容进行增改：

  * 在目录中增加“🧪 Full Test Runner（统一入口）”。

  * 在“Quick Start”与“End-to-End Run”中增加统一入口命令。

  * 在“Results & Evaluation”中补充 `summary.json` 字段说明与示例。

  * 在“Troubleshooting”中新增与统一入口与汇总相关的注意事项。

* 完成后不更改其他文件与结构。

